# libs/ordy-agent — Agent orchestrator (Phase 5–6)

The LangGraph supervisor graph (five roles), typed conversation state, prompt
bundles, and the model router. Packaged as a library so it can later be extracted
from `services/api` into its own deployable without a rewrite (ADR-002/004).

See [docs/03-agent-engine.md](../../docs/03-agent-engine.md). Not yet implemented.
