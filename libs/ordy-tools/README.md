# libs/ordy-tools — Tool & action framework (Phase 6)

ToolSpec types, the platform tool catalog, the deterministic validators, and the
executor adapter interfaces (native / rest / pos / browser). This is the only path
from a model's intent to a real side effect (ADR-010).

See [docs/03-agent-engine.md §4](../../docs/03-agent-engine.md). Not yet implemented.
